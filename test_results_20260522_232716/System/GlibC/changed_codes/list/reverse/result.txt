QCP_VERIFIED: YES

```c
#include "/home/tzh66/qcp_skill/infra/dll_nodata_def.h"

struct list *reverse(struct list *p)
    /*@ Inv Assert
        dlistrep_shape(v, w) *
        dlistrep_shape(w, v)
     */
    while (v) {
        t = v->next;
        v->next = w;
        v->prev = t;
        w = v;
        v = t;
    }
    return w;
}
```
