QCP_VERIFIED: NO
```c
#include "/home/tzh66/qcp_skill/infra/dll_nodata_def.h"

struct list * append(struct list * x, struct list * y)
/*@ With x_prev
	Require dlistrep_shape(x, x_prev) * dlistrep_shape(y,0)
  Ensure dlistrep_shape(__return,x_prev)
 */;

struct list *multi_append(struct list *x, struct list *y, struct list *z)
/*@ Require dlistrep_shape(x,0) * dlistrep_shape(y,0) * dlistrep_shape(z,0)
    Ensure  dlistrep_shape(__return,0)
 */
{
    struct list *t, *u;
    if (x == 0) {
        t = append(y , z);
        return t;
    } else {
        t = x;
        u = t->next;
        /*@ Inv Assert x != 0 && t != 0 && u != 0 &&
            dllseg_shape(x, 0, t, u) * dlistrep_shape(u, t) *
            dlistrep_shape(y, 0) * dlistrep_shape(z, 0)
         */
        while (u) {
            if (y) {
              t -> next = y;
              y -> prev = t;
              t = y;
              y = y -> next;
              t -> next = u;
              u -> prev = t;
              if (y) {
                y -> prev = 0;
              }
              t = u;
              u = u -> next;
            }
            else {
              u = append(u , z);
              t -> next = u;
              return x;
            }
        }
        u = append(y,z);
        t -> next = u;
      	if (u) {
          u -> prev = t;
        }
        return x;
    }
}
```
