QCP_VERIFIED: YES
RETRIES: 0
MANUAL_ADMITTED: 0
AUTO_ADMITTED: 0

```c
#include "/home/tzh66/qcp_skill/infra/dll_nodata_def.h"

struct list *iter(struct list *l)
/*@ With pstPrev
    Require dlistrep_shape(l, pstPrev)
    Ensure  dlistrep_shape(__return, pstPrev)
 */
{
    struct list *p;
    p = l;
    /*@ Inv Assert exists p_prev,
        dllseg_shape(l, pstPrev, p_prev, p) *
        dlistrep_shape(p, p_prev)
     */
    while (p) {
        p = p->next;
    }
    return l;
}
```