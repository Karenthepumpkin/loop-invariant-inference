QCP_VERIFIED: YES
```c
#include "/home/tzh66/qcp_skill/infra/dll_nodata_def.h"

struct list *reverse(struct list *p)
{
    struct list *v;
    struct list *w;
    struct list *t;
    v = p;
    w = (struct list*)0;
    /*@ Inv Assert exists v_prev,
        dlistrep_shape(w, v) *
        dlistrep_shape(v, v_prev)
     */
    while (v) {
        t = v->next;
        v->next = w;
        v->prev = t;
        w = v;
        v = t;
    }
    return w;
}
```
