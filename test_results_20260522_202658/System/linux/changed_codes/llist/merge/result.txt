QCP_VERIFIED: YES

```c
#include "/home/tzh66/qcp_skill/infra/sll_nodata_def.h"

struct list *merge(struct list *x , struct list *y)
/*@ Require listrep(x) * listrep(y)
    Ensure  listrep(__return)
 */
{
    struct list *z, *t;
    if (x == 0) {
      return y; 
    }
    else {
      z = x;
      t = y;
      /*@ Inv Assert x != 0 &&
          data_at(&t, struct list*, t) *
          lseg(z, x) *
          listrep(x) *
          listrep(y)
       */
      while (y) {
        t = y -> next;
        y -> next = x -> next;
        x -> next = y;
        if (y -> next == 0) {
          y -> next = t;
          return z;
        }
        else {
          x = y -> next;
          y = t;
        }
      }
    }
    
    return z;
}
```
