QCP_VERIFIED: NO

The loop invariant could not be verified within the retry limit. The merge function performs cross-list pointer manipulation (inserting nodes from y-list into z-list while advancing both traversal pointers), which creates verification challenges that the available QCP dll_nodata strategies cannot resolve.

The best invariant attempted was:

z != 0 && x != 0 &&
dllseg_shape(z, 0, x->prev, x) *
dlistrep_shape(x, x->prev) *
dlistrep_shape(y, 0) *
has_permission(&t)

This captures:
- The merged prefix from z to x (excluding x) via dllseg_shape
- The remaining x-list from x via dlistrep_shape(x, x->prev)
- The remaining y-list via dlistrep_shape(y, 0)
- Permission for t (required for loop body usage)

QCP fails with "Cannot derive the precondition of Memory Read" during the loop body verification at `t = y->next`, indicating the strategies cannot find y->next in the unfolded state despite the predicate being present.

======

Analysis:

The merge function takes two doubly-linked lists (x and y) and interleaves them:
- Pre-loop: z = x (saves head), t = y
- Loop (while y): Inserts y node after x, advances x to old x->next, advances y to old y->next
- Early return if y->next == 0 (x-list exhausted), else continues
- Post-loop: Returns z (original head of merged list)

Recording points (3 found):
- RP_pre_loop: dlistrep_shape(x@pre, 0) * dlistrep_shape(y@pre, 0)
- RP_iter_2: After 1 merge — merged prefix x@pre->y@pre->y_297(=x), remaining y=y_292
- RP_iter_3: After 2 merges — merged prefix extended to include y_292, remaining y=y_305

Key findings:
1. Variable `t` must appear in the invariant (QCP requirement for loop body variables)
2. `has_permission(&t)` is needed to track t but conflicts with stack bindings
3. Using x->prev in both dllseg_shape (as yp parameter) and dlistrep_shape (as prev parameter) causes dup_data_at errors due to QCP creating overlapping ownership
4. Without dllseg (simple dlistrep approach), QCP cannot locate x in z's list for body verification
