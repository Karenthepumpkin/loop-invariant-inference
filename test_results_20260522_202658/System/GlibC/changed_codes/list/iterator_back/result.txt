QCP_VERIFIED: NO

== Final invarianted file (tmp/iter_back_inv.c) ==

#include "/home/tzh66/qcp_skill/infra/dll_nodata_def.h"

struct list *iter_back(struct list *l, struct list *head)
/*@ With l_prev
      Require dllseg_shape(head, 0, l_prev, l) * dlistrep_shape(l, l_prev)
    Ensure  dlistrep_shape(__return, 0)
 */
{
    struct list *p;
    if (l == 0) {
      return head;
    }
  	else {
    	p = l;
     /*@ Inv Assert p != 0 && head != 0 &&
        dllseg_shape(head, 0, p->prev, p) * dlistrep_shape(p, p->prev) */
    	while (p != head) {
      	  p = p -> prev;
      }
    }
    return p;
}

== Verification Summary ==

Goal file: tmp/iter_back_goal.v
Proof auto: tmp/iter_back_proof_auto.v — 2 admitted (safety_wit_1, return_wit_2)
Proof manual: tmp/iter_back_proof_manual.v — 3 admitted (entail_wit_1, entail_wit_2, return_wit_1)
Total VCs: 5, Admitted: 5, Proven: 0

== Attempts ==

Attempt 1 — Invariant: dllseg_shape(head, 0, p->prev, p) * dlistrep_shape(p, p->prev)
  Result: 3 entailment VCs admitted (entail_wit_1, entail_wit_2, return_wit_2)
  Root cause: Strategy priority ordering — Rule 8 (cancel dlistrep, priority 5) fires before
  Rule 5 (unfold dlistrep, priority 6), leaving unmatched data_at predicates.

Attempt 2 — Invariant: dlistrep_shape(head, 0)
  Result: Safety failure — cannot derive precondition for p->prev memory read.
  Root cause: Invariant does not connect p to the list structure.

Attempt 3 — Invariant: dllseg_shape(head, 0, l_prev, l) * dlistrep_shape(l, l_prev)
  Result: Safety failure — same as Attempt 2.
  Root cause: No PROP equality linking p's symbolic value to logical variable l,
  preventing strategy matching.

Attempt 4 (retry 1) — Invariant: p != 0 && head != 0 && dllseg_shape(head, 0, p->prev, p) * dlistrep_shape(p, p->prev)
  Result: All 5 VCs admitted (safety_wit_1, return_wit_2 auto; entail_wit_1, entail_wit_2,
  return_wit_1 manual). Strategy cannot discharge the three loop-related entailments.
  Root cause: Same as Attempt 1 — strategy priority ordering prevents proof.
  The added head != 0 PROP constraint did not change the outcome.

== Root Cause Analysis ==

The invariant dllseg_shape(head, 0, p->prev, p) * dlistrep_shape(p, p->prev) is semantically
correct for this function. However, the dll_nodata strategy system (14 rules) cannot
discharge the proof obligations because:

1. At loop entry, the precondition has dlistrep_shape(l, l_prev) which must be unfolded
   to produce data_at(l->prev, l_prev) and dllseg_shape(head,0,l_prev,l). But Rule 8
   (cancel dlistrep, priority 5) fires before Rule 5 (unfold, priority 6), consuming
   the dlistrep before it can be unfolded.

2. The predicate reference confirms that dllseg_shape(x, xp, yp, y) represents a segment
   from x to y exclusive, where the last node in the segment is yp. When p advances to
   p->prev, the invariant shifts: what was previously dllseg now becomes dlistrep,
   requiring simultaneous unfold/fold that the strategies cannot express.

3. There are no fold rules in the strategy set — once dlistrep is unfolded to
   data_at(dlistrep(data_at(...))) patterns, the partial consumption leaves
   orphaned data_at predicates that cannot be re-folded into dlistrep.

== Conclusion ==

The loop-invariant-inference pipeline successfully inferred the correct semantic invariant,
but the QCP strategy-based prover cannot automatically verify it for this traversal pattern.
Manual Coq proofs would be required for the three entailment VCs.
