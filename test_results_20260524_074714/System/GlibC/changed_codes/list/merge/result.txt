QCP_VERIFIED: YES
```c
#include "/home/tzh66/qcp_skill/infra/dll_nodata_def.h"

struct list *merge(struct list * x, struct list * y)
/*@ With x_prev
    Require dlistrep_shape(x, x_prev) * dlistrep_shape(y, 0) && x != 0
    Ensure  dlistrep_shape(__return, x_prev)
 */
{
    struct list *t, *z;
    z = x;
    /*@ Inv Assert exists p,
        x != 0 &&
        dllseg_shape(z, x_prev, p, x) *
        dlistrep_shape(x, p) *
        dlistrep_shape(y, 0) *
        has_permission(&t)
     */
    while (y) {
        t = y -> next;
        y -> next = x -> next;
        y -> prev = x;
        x -> next = y;
        if (y -> next == 0) {
          y -> next = t;
          return z;
        }
        else {
          x = y -> next;
          x -> prev = y;
          y = t;
          if (t) {
            t -> prev = 0;
          }
        }
    }
    return z;
  }
```
