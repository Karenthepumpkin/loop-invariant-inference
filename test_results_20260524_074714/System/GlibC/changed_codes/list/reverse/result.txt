QCP_VERIFIED: YES
```c
#include "/home/tzh66/qcp_skill/infra/dll_nodata_def.h"

struct list *reverse(struct list *p) {
    struct list *w = 0;
    struct list *v = p;
    struct list *t;
    /*@ Inv Assert dlistrep_shape(w, v) * dlistrep_shape(v, w) */
    while (v) {
        t = v->next;
        v->next = w;
        v->prev = t;
        w = v;
        v = t;
    }
    return w;
}
```